{
    "buttons": [
        {
            "name":"MyButton",
            "script":"dockerfiel.bat"
        },
        {
            "name": "john",
            "script": "del .git && git init && git add . && git commit -m 'add' &&  git remote add origin https://github.com/mail2julian/butt.git && git push -u origin master"
            
        }
    ]
}